(()=>{const root=document.querySelector('.flow-carousel');if(!root)return;const track=root.querySelector('.flow-carousel-track');const slides=[...track.querySelectorAll('img')];const status=root.querySelector('.flow-carousel-status');let index=0,timer;const show=next=>{index=(next+slides.length)%slides.length;track.style.transform=`translateX(${-index*100}%)`;status.textContent=`${index+1} / ${slides.length}`};const start=()=>{clearInterval(timer);timer=setInterval(()=>show(index+1),3000)};root.querySelector('.previous').addEventListener('click',()=>{show(index-1);start()});root.querySelector('.next').addEventListener('click',()=>{show(index+1);start()});root.addEventListener('pointerenter',()=>clearInterval(timer));root.addEventListener('pointerleave',start);document.addEventListener('visibilitychange',()=>document.hidden?clearInterval(timer):start());show(0);start()})();
